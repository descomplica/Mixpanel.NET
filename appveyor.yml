version: 1.0.{build}
configuration: Release
platform: Any CPU

before_build:
- nuget restore src\Mixpanel.NET\Mixpanel.NET.sln

build:
  verbosity: minimal
  publish_nuget: true
  
test: off

nuget:
  account_feed: true
